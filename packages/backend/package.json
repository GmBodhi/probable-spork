{
  "name": "backend",
  "version": "1.0.0",
  "main": "index.js",
  "type": "module",
  "license": "MIT",
  "scripts": {
    "start": "node build",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "swc src -d build",
    "dev": "nodemon --exec \"swc src -d build && node build\" --watch src"
  },
  "dependencies": {
    "express": "^4.18.1",
    "sequelize": "^6.21.3"
  },
  "devDependencies": {
    "@swc/cli": "^0.1.57",
    "@swc/core": "^1.2.222",
    "@types/node": "^18.6.3",
    "nodemon": "^2.0.19",
    "typescript": "^4.7.4"
  }
}
